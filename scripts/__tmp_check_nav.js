// ⚠️ ไฟล์ทดสอบชั่วคราว (สร้างระหว่างตรวจ Navigation consistency 2026-08-10)
// ลบไฟล์นี้ทิ้งได้เลย — sandbox ลบเองไม่ได้เพราะสิทธิ์ mount ไม่อนุญาต unlink
// เนื้อหาจริงของ checker อยู่ที่ "check-nav-consistency.js" ใน outputs folder ที่ Claude ส่งให้ Lin ตรวจ
// (ยังไม่ได้ push/commit อะไร — ไฟล์นี้ไม่มีผลต่อเว็บที่ deploy จริง เพราะไม่ถูกเรียกใช้จากที่ไหนเลย)
