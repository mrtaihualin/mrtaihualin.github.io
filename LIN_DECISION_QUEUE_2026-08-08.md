# คิวตัดสินใจของ Lin — 2026-08-08

> ไฟล์นี้เป็นแค่ "รายการรอ Lin ตัดสินใจ" รวบรวมจากหลายแชท — ไม่มีการแก้โค้ด/deploy/รัน SQL ใดๆ จากไฟล์นี้ทั้งสิ้น
> Lin ไล่ดูทีละข้อได้ตามจังหวะของตัวเอง ตัดสินใจแล้วไม่ต้องตอบในนี้ — กลับไปสรุปที่แชท Dispatch เอง

---

## A. Account Auth deploy prep — สิ่งที่บล็อกการตรวจสอบ

**1. `.git/index.lock` ค้างอยู่ในเครื่อง — ปิดงาน (Lin ลบแล้ว)**
ไฟล์ขนาด 0 ไบต์ สร้างเวลา 19:45:57 วันนี้ — ลบได้ไหม?
ต้องเช็คบนเครื่อง Mac จริงของ Lin เอง: เปิด GitHub Desktop ดูว่า push/commit ค้างพร้อม error "index.lock" ไหม
- ถ้าค้างจริง → ลบไฟล์นี้ผ่าน Terminal บนเครื่องจริง (ห้ามลบผ่าน sandbox ใดๆ)
- ถ้า GitHub Desktop ทำงานปกติ → ไฟล์นี้น่าจะเป็นเศษที่ไม่มีผลอะไร ลบทิ้งหรือปล่อยไว้ก็ได้

**2. Account Audit Log + line-login function — รันแล้วหรือยัง? — ปิดงาน (ยืนยันแล้ว)**
> อัปเดต: ตาราง `account_audit_log` เช็คแล้ว **มีจริง** (query `information_schema.tables` เจอแถว) · `line-login` function เช็ค Dashboard แล้ว **deploy แล้วจริง** ("6 hours ago" ตอนเช็ค · มี 4 invocations ตั้งแต่ deploy ล่าสุด ไม่มี error เลย) → ทั้งสองอย่างเสร็จแล้ว ไม่ต้องรอ Lin ตอบเรื่องนี้อีก
มีหลักฐานเป็นไทม์ไลน์ที่บ่งชี้ว่า "น่าจะเสร็จแล้ว" แต่ไม่เคยมีการยืนยันตรงจาก Lin เอง:
- `MAINTENANCE.md` เขียนไว้เวลา 16:15 ว่า "รอ Lin รัน SQL สร้างตาราง `account_audit_log`"
- ไฟล์เอกสารอัปเดตเวลา 18:21 กลับไม่ได้ระบุเรื่องนี้เป็นงานค้างอีกแล้ว
- มีบันทึกส่งต่องานเวลา 19:49 บอกว่าทำเสร็จแล้ว
- แต่ `MAINTENANCE.md` เองยังคงเขียนว่า "รอรัน" (ข้อมูลเก่าค้าง)

→ ต้องการคำตอบตรงๆ: **รันตาราง `account_audit_log` + deploy ฟังก์ชัน `line-login` ใหม่แล้วหรือยัง (ใช่/ไม่ใช่)**

---

## B. Account Auth deploy prep — ต้องตัดสินใจก่อน deploy 3 Edge Functions

**3. `account-export` — ตัดสินใจแล้ว: deploy ได้**
โค้ดเสร็จสมบูรณ์ ไม่มีคำถามค้าง — Lin ยืนยันแล้วว่า **deploy ได้เลย** (การ deploy จริงให้ทำที่แชท Dispatch)

**4. `account-delete` — ตัดสินใจแล้ว (2026-08-08) — ให้ Dispatch ทำ analysis/design/local prep ต่อ (ห้าม deploy/รัน SQL/แตะ production)**

- **(a) Cooldown 7 วัน** (เปลี่ยนจากคำตอบเดิม):
  - ผู้ใช้ส่งคำขอลบบัญชี → เข้าสถานะ pending deletion
  - ภายใน 7 วันยกเลิกคำขอลบได้
  - ครบ 7 วันจึงลบถาวร
  - ออกแบบให้ปลอดภัย ป้องกันการลบผิดบัญชี
  - ⚠️ ถ้ามี behavior สำคัญที่ยังต้องตัดสินใจก่อนเขียนส่วนที่กระทบ flow จริง **ต้องรายงาน Lin ก่อน ห้ามเดาเอง**

- **(b) `payout_ledger`:**
  - ข้อมูลที่จบแล้ว (จ่ายแล้ว/ปฏิเสธแล้ว) **เก็บไว้ก่อน ห้ามลบ ห้าม anonymize เอง**
  - เรื่อง retention/จัดการระยะยาว ตัดสินใจภายหลัง

- **(c) แจ้งอีเมล:**
  - ยื่นคำขอลบบัญชี → ต้องส่งอีเมลแจ้งทันที
  - บอกว่าจะถูกลบถาวรเมื่อครบ 7 วัน + วิธียกเลิกคำขอภายในช่วงนั้น
  - ⚠️ ต้องตรวจระบบอีเมลที่มีอยู่ก่อนใช้ **ห้ามสร้างระบบใหม่โดยเดา**

- **(d) `star_ledger`:**
  - ต้องตรวจ schema + foreign key + โค้ดที่อ่าน/เขียนจริงก่อน ว่ามี PII หรือข้อมูลเชื่อมกลับไปหาผู้ใช้ไหม
  - **ยังไม่แก้/ลบข้อมูลใดๆ จนกว่าจะรายงานผลให้ Lin ตัดสินใจ**

> ขอบเขตรอบนี้: analysis/design/local preparation ทำได้ **ห้าม deploy, รัน SQL, หรือแตะ production** — ถ้า cooldown 7 วันต้องเปลี่ยน database schema/cron/scheduled job/deletion architecture ต้องหยุดตรงจุดนั้นก่อน แล้วสรุปสิ่งที่ต้องเปลี่ยนมาให้ Lin ตัดสินใจ

**5. `account-unlink`** — ยังไม่แก้โค้ด — **(a) ปิดงาน (ตรวจครบแล้ว 2026-08-08 — ตรวจ source code แทนบัญชีจริง เพราะยังไม่มีเคส LINE-only สมัครใหม่เกิดขึ้นจริงในระบบเลย)**

  1. Query หาบัญชี LINE-only จริงใน `auth.users` (`email like 'line-%@users.line.invalid'`) → **0 แถว** ยังไม่มีใครสมัครใหม่ผ่าน LINE ล้วนๆ เลยจริงๆ
  2. ไล่โค้ดแทน: `line-login/index.ts` สร้าง synthetic email ด้วย `supabase.auth.admin.createUser({email:'line-'+lineUserId+'@users.line.invalid'})` — **ตรงกับที่สมมติไว้ทุกจุด** และ `account-unlink/index.ts` เช็คด้วย regex เดียวกันเป๊ะ (`SYNTHETIC_LINE_EMAIL_RE`)
  3. อีเมลนั้น login จริงไม่ได้ (ไม่มีกล่องจดหมายจริง) — ล็อกอินจริงไปทาง flow LINE เอง (magic link ภายใน)
  4. **โค้ดนับช่องทาง login ถูกต้องแล้ว** — `account-unlink/index.ts` กรอง synthetic email ออกจากการนับ "ช่องทางจริง" อยู่แล้ว (`realNative = annotatedNative.filter(idn => !idn._synthetic_line_email)`) + มี error code เฉพาะ (`cannot_unlink_synthetic_email`, `would_leave_zero_login_methods`) + เช็คไขว้กับตาราง `line_identities` ถ้าข้อมูลไม่ตรงกัน log เตือนให้ตรวจมือ

  → สมมติฐานของโค้ดถูกต้องครบ ไม่มีจุดเสี่ยง ที่เหลือแค่ "ยังไม่มีเคสจริงให้ทดสอบ end-to-end" (ไม่ใช่ปัญหาโค้ด แค่ยังไม่มีข้อมูลจริง) — (b) REST endpoint ยังไม่ทดสอบตามที่ Lin สั่งเลื่อนไว้เอง

- **(b) REST endpoint ของ unlink** — **ยังไม่ทดสอบรอบนี้** (Lin สั่งเลื่อนไปก่อน)

**6. ไฟล์ SQL ที่พร้อมรัน (ยังไม่มีไฟล์ไหนถูกรันเลย — ตรวจจากสถานะไฟล์ตรงๆ)**
- `supabase/sql/2026-08-08_anon_spam_protection.sql` — เพิ่มการกันสแปมให้ตาราง `anon_game_events` + `leads` — **ปิดงาน (รันจริงสำเร็จครบแล้ว 2026-08-08)** รัน [A] สร้างฟังก์ชัน rate-limit + [B] แก้ policy สำเร็จ · verify พบ policy ทั้ง 2 ตารางผูก `with_check` เข้ากับฟังก์ชัน `anon_game_events_rate_ok(anon_id)` / `leads_rate_ok(email)` ถูกต้องแล้ว
- `supabase/sql/2026-08-08_drop_duplicate_tone_sessions_policy.sql` — ลบ RLS policy ที่ซ้ำซ้อนบนตาราง `tone_sessions` — **ปิดงาน (รันจริงสำเร็จครบแล้ว 2026-08-08)** สำรองฐานข้อมูลก่อนด้วย workflow `backup-database-to-drive` (Success) → รัน `drop policy if exists "read own sessions"` → verify แล้วเหลือ SELECT policy เดียว (`select own sessions`) + `insert own sessions` ยังอยู่ครบ + ยืนยัน `read own sessions` หายไปจริง (0 แถว)
- `supabase/sql/2026-08-08_leaderboard_score_bounds.sql` — ปิดช่องโหว่การโกงคะแนน leaderboard ของจริง — **ปิดงาน (รันจริงสำเร็จครบแล้ว 2026-08-08)**
  > พบ 4 แถวปลอมในการทำ PRECHECK ก่อนรัน — `user_id`=`colo808pp@gmail.com` (บัญชีทดสอบ/ยิงลองครั้งเดียว 25 มิ.ย. ไม่เคย sign in ซ้ำ) คะแนน/total เท่ากันเป๊ะ insert ห่างกัน <200ms ยืนยันเป็นของปลอมยิง API ตรงๆ (คะแนนสูงสุดจริงในเกม ~405 แต่แถวนี้ได้ 44250) · เช็ค `star_ledger` แล้วไม่มีแถวเลย = ไม่เคยแจกดาว/รางวัลจากคะแนนปลอมนี้ กระทบแค่หน้ากระดานผู้นำ
  > ขั้นตอนที่ทำจริง: (1) สำรอง 4 แถวลงตาราง `_backup_20260808_fake_tone_sessions` (เปิด RLS) (2) ลบ 4 แถวปลอมทิ้ง (3) รัน PRECHECK ซ้ำได้ 0 ครบทั้ง 3 query (4) รัน [A] สร้างฟังก์ชัน rate-limit + [B] แก้ CHECK constraint + [C] แก้ policy (5) รัน [D] verify ผ่านครบทั้ง 4 เช็ค (ฟังก์ชัน 1 เวอร์ชันต่อชื่อ+security_definer=true, execute grant ครบ anon/authenticated, policy ผูก rate_ok จริง, constraint ครบ 4 ชื่อตรงนิยาม)
  > → ไม่ต้องรอ Lin ตอบเรื่องนี้อีก เสร็จสมบูรณ์แล้ว

---

## C. คิวตรวจจากรายงานตรวจ (Audit handoff) — ปิดครบแล้ว

**7. ไฟล์ที่ถูกแก้วันนี้โดยไม่รู้ที่มา — ปิดงาน (ตรวจ diff จริงแล้ว ไม่ใช่ปริศนา)**
`MAINTENANCE.md` และ `supabase/functions/calendar-schedule-sync-cron/index.ts` ถูกแก้ไขวันนี้ — เปิด GitHub Desktop → Changes ดู diff จริงแล้ว:
- `MAINTENANCE.md`: มีแชท "รอบตรวจสอบ audit อิสระ" แก้บันทึกที่ค้างผิดให้ตรงสถานะจริง (ยืนยันว่า `account_audit_log` รันแล้ว + `line-login` deploy แล้ว — ตรงกับที่ยืนยันซ้ำใน A.2 ของไฟล์นี้พอดี) ส่วนบรรทัดอื่นๆ ในไฟล์ก็ระบุที่มาชัดเจนในตัวเอง (เรื่องปุ่มแชร์, เรื่องเชื่อม LINE)
- `calendar-schedule-sync-cron/index.ts`: แก้แค่ comment ในโค้ด ชี้ path ไฟล์ SQL ตั้งค่า cron ใหม่ (จาก `2026-07-17_pg_cron_calendar_schedule_sync.sql` → `2026-08-07_p1-06_cron_vault.sql`) ตรงกับที่ CLAUDE.md บันทึกไว้แล้วว่า P1-06 ย้ายค่าลับ cron เข้า Supabase Vault — ไม่ใช่ logic เปลี่ยน แค่ housekeeping ต่อเนื่องจากงานที่รู้ที่มาอยู่แล้ว
- **สรุป: ไม่มีแชท/session ที่ 3 ที่ไม่รู้จักเลย** ทั้ง 21 ไฟล์ที่เปลี่ยนวันนี้อธิบายได้หมดจากงานที่ Lin คุยอยู่แล้ว (Account Auth prep, แชทนี้, งาน cron vault cleanup)

**8. เลขไฟล์ซ้ำกัน (ไม่กระทบอะไร แค่แจ้งไว้) — ปิดงาน**
มีเอกสารแผนงาน 2 ไฟล์ที่ถูกตั้งเลข "57" ซ้ำกันโดยคนละแชท (`57_MASTER_AUDIT_HANDOFF...md` และ `57_P5-C_...md`) — ไม่กระทบการทำงานอะไร Lin เคยบอกว่า "ยังไม่ต้องเปลี่ยนชื่อตอนนี้" — แค่แจ้งไว้เฉยๆ ว่ายังค้างอยู่ เผื่อ Lin อยากจัดการเมื่อไหร่ก็ได้

---

## D. Unlink Google/Email — บันทึกขัดแย้งกัน ต้องยืนยันครั้งสุดท้าย

**9. กฎการ unlink ผู้ให้บริการล็อกอิน**
ในแชทหนึ่ง Lin เคยตอบว่า "อนุญาตให้ unlink ผู้ให้บริการใดก็ได้ รวมถึงวิธีสมัครดั้งเดิม ตราบใดที่ยังเหลืออย่างน้อย 1 วิธีล็อกอิน"
แต่ในอีกรายการติดตาม ("งานที่ Lin ต้องทำเอง") กลับบันทึกคำถามเดียวกันนี้ว่า "ยังไม่ได้ตอบ"
คำสั่งล่าสุดของ Lin บอกให้ถือว่าเรื่องนี้ยังเปิดอยู่ รอการทบทวนเรื่อง "โครงสร้าง Auth" ก่อน
→ ต้องการคำยืนยันสุดท้าย ชัดเจนครั้งเดียวว่ากฎจริงคืออะไร กันไม่ให้ต้องมาเถียงเรื่องนี้ซ้ำอีก

---

## E. แก้บั๊กปุ่มแชร์บทความ (article share button) — รอ Lin ทดสอบจริง

**10. ทดสอบปุ่มแชร์**
โค้ดแก้แล้วและ push ไปแล้ว (bump cache-buster v17→v18 ใน 76 ไฟล์)
Lin ต้อง hard-refresh (Cmd+Shift+R) แล้วทดสอบปุ่มแชร์ 3 จุด:
- iPhone Safari (ควรขึ้นเมนูแชร์ของระบบ OS)
- desktop Safari (ควรขึ้น popup FB/LINE ตรงๆ ไม่ผ่านเมนูระบบก่อน)
- desktop Chrome (เหมือน Safari)
→ รายงานผลผ่าน/ไม่ผ่านของแต่ละจุด

---

## F. สี่เกมรวมเป็นเดียว (mix.html) — รอ Lin ตรวจด้วยตา

**11. ตรวจหน้า mix.html**
เดิมมีบั๊กเรื่อง layout คีย์บอร์ด (4 แถวยุบรวมเป็นบรรทัดเดียว) และเรื่องสีทอง — แก้แล้วทั้ง 2 จุด
Lin เปิดไฟล์ใหม่แล้วดูว่าตอนนี้ถูกต้องไหม (คีย์บอร์ดแยก 4 แถวชัดเจน + สีทอง premium `#8B6310` บนหัวข้อ)
⚠️ ไฟล์นี้อยู่ในเครื่องอย่างเดียว ยังไม่ได้ push ขึ้น GitHub

---

## G. แบนเนอร์ขอความยินยอมคุกกี้ (Cookie consent) — เลื่อนไว้ก่อน ไม่ต้องทำอะไรตอนนี้

**12. สถานะ: ตัดสินใจแล้ว รอทำทีหลัง — ปิดงาน**
Lin ตัดสินใจแล้วว่า: ใช่ จะสร้างแบนเนอร์คุกกี้ในอนาคต (เว็บใช้ GA4 + Microsoft Clarity + YouTube embed แบบไม่ใช่ privacy mode ซึ่งตอนนี้ privacy policy ยังไม่ได้ระบุชื่อพวกนี้ตรงๆ)
แต่ตั้งใจแยกเป็นงานอนาคตต่างหาก — ยังไม่เริ่ม ไม่มีอะไรต้องทำตอนนี้ ใส่ไว้ในนี้เพื่อให้ครบเท่านั้น

---

## H. "งานที่ Lin ต้องทำเอง" — รายการงานที่ต้องทำด้วยมือ (ติดตามอยู่ในแชทเฉพาะแล้ว รวมมาไว้ให้ดูภาพเดียว)

**13.** เช็คประวัติการรันของ GitHub Actions workflow สำรองข้อมูล (เคยรันสำเร็จจริงไหม?) — **ปิดงาน** ยืนยันแล้ว 2026-08-08: กด Run workflow `backup-database-to-drive` เอง ผลออกมา Success (2m19s) มีแค่ warning Node.js 20 deprecated ไม่กระทบข้อมูล
**14.** ยืนยันว่า secret ทั้ง 6 ตัวที่ workflow สำรองข้อมูลต้องใช้ ถูกตั้งไว้ใน GitHub repo settings จริงหรือไม่
**15.** เช็คมือถือ: เคยได้รับแจ้งเตือน LINE จากระบบสำรองข้อมูลจริงไหม?
**16.** ดูสูตรคิดคะแนนจริงในเกมวรรณยุกต์/เกมอ่าน (context ที่ต้องใช้เพื่อกำหนดเพดานคะแนน leaderboard อย่างปลอดภัย) — ไม่เร่งด่วน
**17.** ติดต่อฝ่ายขาย ECPay/NewebPay สอบถามค่าธรรมเนียม + เงื่อนไขการเก็บเงินแบบรายเดือน — ยังไม่เร่งด่วนจนกว่าจะพร้อมขายจริง
**18.** คุยกับนักบัญชีจริงเรื่องภาษี (เช่น ลูกค้าฮ่องกงนับเป็นการส่งออกหรือไม่) — ต้องใช้นักบัญชีจริง ไม่เร่งด่วน
**19.** เช็คหน้า "Sessions" ใน Supabase Auth — session/refresh token หมดอายุกี่วัน (ไม่บังคับ เช็คเร็วๆ ก็ได้)

---

Lin, ไล่ดูทีละข้อตามจังหวะของตัวเองได้เลย ไม่ต้องตอบในนี้ — กลับไปที่แชท Dispatch แล้วสรุปสิ่งที่ตัดสินใจเองตอนพร้อม
