# 📇 สารบัญ — ฟังก์ชันในฐานข้อมูล ตัวไหนอยู่ไฟล์ไหน

> สร้าง 2026-07-31 · **อ่านไฟล์นี้ก่อนแก้ SQL ทุกครั้ง**
> ทำไมต้องมี: ปัญหาใหญ่ 2 ครั้งที่ผ่านมาเกิดจากเรื่องเดียวกัน — **ฟังก์ชันตัวเดียว มีโค้ดอยู่หลายไฟล์
> แล้วไม่มีใครรู้ว่าตัวไหนใหม่สุด** → เผลอรันไฟล์เก่าทับ = ของที่แก้ไปแล้วหายเงียบๆ หรือเว็บล่มทั้งระบบ

---

## 🔑 กฎ 3 ข้อ (ตั้ง 2026-07-31 ตามที่ Lin สั่ง)

1. **ฟังก์ชัน 1 ตัว = มีต้นฉบับล่าสุดอยู่ไฟล์เดียวเท่านั้น** — ดูได้จากตารางข้างล่าง
2. **แก้ฟังก์ชันไหนเสร็จ ต้องมาอัปเดตตารางข้างล่างทันที** ไม่งั้นสารบัญนี้จะกลายเป็นตัวหลอกแทนที่จะช่วย
3. **ก่อนกด Run ไฟล์ SQL ไหน เปิดดูหัวไฟล์ก่อนเสมอ** — ไฟล์ที่เลิกใช้แล้วจะมีป้าย 🛑 หรือ ⚠️ อยู่บรรทัดแรก

---

## ✅ ตารางหลัก — ฟังก์ชันที่ใช้งานจริงตอนนี้

| ฟังก์ชัน | ทำอะไร | 📄 ต้นฉบับล่าสุดอยู่ที่ |
|---|---|---|
| `submit_class_request` | ประตูทางเข้าเดียวของ **ทุกคำขอ** (ยกเลิก / ขอเลื่อน / ขอเพิ่มคาบ) | 🆕 `sql/2026-08-02_rpc_guards_merged.sql` |
| `is_teacher_caller` | ตัวช่วย — เช็คว่าคนที่เรียกอยู่ตอนนี้เป็นครูจริงไหม (อ่านอีเมลจากการล็อกอิน) | `sql/2026-07-30_submit_class_request_consolidated.sql:62` |
| `student_update_own_request` | นักเรียนแก้ / ถอนคำขอของตัวเอง | 🆕 `sql/2026-08-02_rpc_guards_merged.sql` |
| `respond_to_offer_as_student` | นักเรียนกด "ตกลง / ไม่สะดวก" กับเวลาที่ครูเสนอ | 🆕 `sql/2026-08-02_rpc_guards_merged.sql` |
| `student_get_own_requests` | นักเรียนอ่านคำขอของตัวเอง | `2026-07-26_student-rpc-add-rate-limit.sql:141` |
| `get_student_by_token` | ดึงข้อมูลนักเรียนจากรหัสบนลิงก์ | `2026-07-26_student-rpc-add-rate-limit.sql:37` |
| `get_student_folder` | ดึงโฟลเดอร์ของนักเรียน | `2026-07-26_student-rpc-add-rate-limit.sql:57` |
| `get_student_attendance` | ดึงประวัติเข้าเรียน | `2026-07-26_student-rpc-add-rate-limit.sql:74` |
| `get_student_payments` | ดึงประวัติการจ่ายเงิน | `2026-07-26_student-rpc-add-rate-limit.sql:89` |
| `get_student_recordings` | ดึงวิดีโอบันทึกคาบเรียน | `2026-07-26_student-rpc-add-rate-limit.sql:104` |
| `get_student_schedule` | ดึงตารางเรียนล่วงหน้า | `2026-07-26_student-rpc-add-rate-limit.sql:119` |
| `slink_client_ip` | ตัวช่วยยามเฝ้าประตู — ดู IP ของคนที่ยิงเข้ามา | `2026-07-26_student-link-rate-limit.sql:37` |
| `slink_rl_check` | ยามเฝ้าประตู — นับว่ายิงเกินโควตาไหม | `2026-07-26_student-link-rate-limit.sql:58` |
| `slink_log_fail` | จดบันทึกคนที่ใช้รหัสมั่ว | `2026-07-26_student-link-rate-limit.sql:90` |
| `notify_line_gate` | ด่านคุมว่าใครส่งข้อความเข้า LINE ของครูได้ | `sql/2026-07-26_notify_line_gate.sql:37` |
| `lego_consume_daily` | โควตาเล่นเกมเลโก้ต่อวัน | `sql/2026-07-26_lego_daily_limits.sql:46` |
| `combined_leaderboard_weekly` | กระดานคะแนนรายสัปดาห์ | `sql/2026-07-17_combined_leaderboard.sql:13` |
| `combined_leaderboard_alltime` | กระดานคะแนนตลอดกาล | `sql/2026-07-17_combined_leaderboard.sql:54` |
| `payout_precheck` | ด่านก่อนจ่ายรางวัล | `phase5-payout-gate.sql:10` |
| `rl_check` | ยามเฝ้าประตูของระบบเกม (คนละตัวกับ `slink_rl_check`) | `phase5-rate-limit.sql:22` |
| 🆕 `game_content_rl_check` | ยามเฝ้าประตูของ Edge Function `game-content` (คนละตัวกับ `rl_check`/`slink_rl_check` — คีย์เป็น text รองรับทั้ง `user:<uuid>` และ `ip:<ip>`) | `sql/2026-08-02_game_content_schema.sql` |

> ทุกที่อยู่ในตารางนับจากโฟลเดอร์ `supabase/` · ตรวจเลขบรรทัดตรงกับไฟล์จริงแล้วเมื่อ 2026-07-31

## 📋 ไฟล์ที่ไม่ใช่ฟังก์ชัน — แต่ต้องรัน

| ไฟล์ | ทำอะไร | จำเป็นแค่ไหน |
|---|---|---|
| 🆕🔴 `sql/2026-08-02_rpc_guards_merged.sql` | **ไฟล์เดียวที่เป็นต้นฉบับของ RPC ฝั่งนักเรียนทั้ง 3 ตัว** (ดูตารางบน) · รวมด่านที่เคยกระจายอยู่ 2 ไฟล์แล้วทับกันเอง + อุดรูใหม่จากรอบตรวจ 2026-08-02 (ด่าน 24 ชม.ข้ามได้ถ้าไม่ส่งวันที่ · `respond_to_offer_as_student` ไม่มี rate limit) · ข้างในมีตาราง `cron_state` + คอลัมน์ `sla_reminder_last_sent_at` มาให้ด้วย (รันซ้ำได้) | **ต้องรัน** · ต้องได้ 3 แถว และช่องที่ขึ้นต้นด้วย "ต้องมี" ต้องเป็น true ทุกช่อง |
| 🆕 `sql/2026-08-02_reschedule_lock_guard_TEST.sql` | ตัวทดสอบด่านรอบ 2 "ห้ามแก้เวลาที่ขอ ตอนครูกำลังจัดการอยู่" — สร้างคำขอทดสอบจริง 2 ใบ แล้วลบทิ้งเองเสมอ · ไม่แตะ Calendar ไม่ส่ง LINE | **ควรรัน** ทันทีหลังรัน `2026-08-01_reschedule_guards.sql` รอบ 2 · ต้องได้ ✅ ครบ 6 แถว |
| 🆕 `sql/2026-08-02_game_content_schema.sql` | สร้างตาราง `game_words`/`game_sentences` (ล็อกจริง — ไม่มี grant ให้ anon/authenticated เลย) + `game_content_rl_check` (ยามเฝ้าประตู) — ใช้คู่กับ Edge Function `game-content` และสคริปต์ `scripts/migrate-game-content.js` (แก้บั๊กเพดานเนื้อหาปลอมที่ `data/words-data.js`/`data/adv-sentences.js`) | **ต้องรันก่อน** deploy Edge Function `game-content` และก่อนรัน `scripts/migrate-game-content.js` ครั้งแรก |
| 🆕 `sql/2026-07-31_backup_action_create.sql` | ขยายด่าน `classroom_calendar_backups_action_check` จาก 3 ค่าเป็น **5 ค่า** — เติม `archive_student` (โค้ดใช้มาตั้งแต่ 2026-07-26 แต่ด่านไม่เคยรับ = ปุ่มเก็บนักเรียนเข้ากรุเก็บกวาดคาบเก่าไม่สำเร็จมาตลอด) + `create` (ปุ่ม ↩️ 復原 ของคาบที่เพิ่งเพิ่ม) | **ควรรัน** · เช็คขั้นที่ 1 ยืนยันแล้ว 2026-07-31 ว่าด่านมีจริงและขาด 2 ค่า · ไม่รัน = ปุ่มกู้คืนคาบที่เพิ่มไม่ขึ้น + ปุ่มเก็บเข้ากรุยังทำงานไม่ครบเหมือนเดิม |
> ⚠️ เลขบรรทัดเลื่อนได้ถ้ามีใครแก้ไฟล์ — ถ้าเปิดไปแล้วไม่ตรง ให้ค้นหาชื่อฟังก์ชันในไฟล์นั้นแทน ตัว "ชื่อไฟล์" คือของที่ต้องเชื่อ

---

## 🛑 ไฟล์ที่เลิกใช้แล้ว — เก็บไว้ดูประวัติ ห้ามกด Run

### กลุ่มที่ 1 — รันแล้ว **เว็บล่มทั้งระบบทันที** (ย้ายเข้ากรุแล้ว 2026-07-31)

อยู่ในโฟลเดอร์ `supabase/sql/เลิกใช้แล้ว_ห้ามรัน/`

| ไฟล์ | ข้างในมีอะไร | รันแล้วเกิดอะไร |
|---|---|---|
| `2026-07-26_submit_class_request_เวอร์ชันเก่า_7และ8ช่อง.sql` | `submit_class_request` แบบ 7 และ 8 ช่องรับค่า | ฟังก์ชันซ้อนกัน → ส่งคำขออะไรไม่ได้เลยทั้งเว็บ + ด่าน 24 ชม.หาย |
| `2026-07-26_force_initiated_by.sql` | `is_teacher_caller` + `submit_class_request` 2 เวอร์ชันเก่า | เหมือนกัน |
| `2026-07-19_allow_add_class_request_type.sql` | `submit_class_request` 2 เวอร์ชันเก่า | เหมือนกัน + ด่าน "ห้ามนักเรียนอ้างว่าเป็นครู" หายด้วย |

**ทำไมถึงล่ม (อธิบายง่ายๆ):** ฐานข้อมูลตัดสินว่าจะเรียกฟังก์ชันตัวไหน จาก "จำนวนช่องรับค่า"
ถ้ามี 2 ตัวที่เข้าได้พอๆ กัน มันจะเลือกไม่ถูก แล้วตอบว่า *"Could not choose the best candidate function"*
= เว็บส่งคำขออะไรไม่ได้เลยสักอย่าง — เกิดขึ้นจริงมาแล้วเมื่อ **2026-07-30**

### กลุ่มที่ 2 — รันแล้ว **ไม่ล่ม แต่ของที่แก้ไปแล้วหายเงียบๆ** (ยังอยู่ที่เดิม มีป้าย ⚠️ หัวไฟล์)

ช่องรับค่าเท่าเดิม จึงเขียนทับได้ ไม่เกิดฟังก์ชันซ้อน — **แต่นั่นแหละคืออันตราย เพราะไม่มี error อะไรเลย**

| ไฟล์ | รันแล้วจะย้อนอะไรหาย |
|---|---|
| 🆕🔴 `sql/2026-08-01_reschedule_guards.sql` | **เลิกใช้แล้ว 2026-08-02** — ข้างในมี `student_update_own_request` ที่ **ขาด 2 ด่าน** (ล็อกบังคับ + cancel ห้ามพกข้อเสนอ) และ `submit_class_request` ที่ด่าน 24 ชม.ข้ามได้ถ้าไม่ส่งวันที่ · รันซ้ำ = ย้อนทุกอย่างของ `2026-08-02_rpc_guards_merged.sql` |
| 🆕🔴 `sql/2026-08-01_cancel_add_guards.sql` | **ส่วนฟังก์ชันเลิกใช้แล้ว 2026-08-02** — ข้างในมี `student_update_own_request` ที่ **ขาดด่าน ★★★** (ห้ามแก้เวลาตอนครูกำลังทำอยู่) · ⚠️ ส่วนบนของไฟล์ (ตาราง `cron_state` + คอลัมน์ `sla_reminder_last_sent_at`) รันซ้ำได้ ไม่มีอันตราย — แต่ของพวกนั้นอยู่ในไฟล์ merged แล้ว ไม่ต้องรันแยก |
| 🆕 `sql/2026-07-30_submit_class_request_consolidated.sql` | ด่าน **"รหัสนักเรียนต้องมีอยู่จริง"** + การเอาชื่อจากฐานข้อมูล (งาน C14) · ⚠️ ชุดทดสอบท้ายไฟล์นั้นใช้ไม่ได้แล้ว (ใช้ token ปลอม ซึ่งตอนนี้ถูกปฏิเสธตั้งแต่ด่านแรก) · **ยกเว้น `is_teacher_caller()` ในไฟล์นั้นยังเป็นของล่าสุด** |
| 🆕 `sql/2026-07-31_submit_verify_token.sql` | การเก็บ `original_time` ลงตารางตอนสร้างคำขอ (ไฟล์นี้ยังไม่เก็บ ยัดทิ้งหลังใช้เช็คด่าน 24 ชม.) — รันไฟล์นี้ซ้ำจะทำให้ต้องกลับไปพึ่งเว็บเติมทีหลังเหมือนเดิม (จุดพังที่เพิ่งอุด) |
| `sql/2026-07-19_student_request_rpcs.sql` | ค่า `declined` (ปุ่ม 婉拒 ของนักเรียนพัง) + ยามเฝ้าประตูกันไล่เดารหัส + ด่าน 24 ชม.ตอนแก้คำขอ |
| `sql/2026-07-20_student_decline_teacher_add.sql` | ยามเฝ้าประตูกันไล่เดารหัส + ด่าน 24 ชม.ตอนแก้คำขอ |
| `sql/2026-07-19_fix_get_student_schedule_limit.sql` | ยามเฝ้าประตูกันไล่เดารหัส |

> 📌 ทั้ง 3 ไฟล์นี้ **ไม่ย้ายเข้ากรุ** เพราะเนื้อหาในไฟล์อธิบายเหตุผลเบื้องหลังไว้ดี และโค้ดเว็บอ้างถึงไฟล์แรกอยู่
> (`classroom/index.html:5538`) — อ่านได้ตามปกติ แค่ห้ามกด Run

---

## 🧬 ประวัติของ `student_update_own_request` (ตัวอย่างว่าทำไมสารบัญนี้จำเป็น)

ฟังก์ชันตัวเดียวนี้ ถูกเขียนทับมา **3 รอบ ใน 3 ไฟล์**:

```
2026-07-19  sql/2026-07-19_student_request_rpcs.sql          ← ตัวแรก
    ↓  เพิ่มค่า offer_status = 'declined'
2026-07-20  sql/2026-07-20_student_decline_teacher_add.sql
    ↓  เพิ่มยามเฝ้าประตูกันไล่เดารหัส (20 ครั้ง/60 วิ)  ← และเก็บ 'declined' ไว้ครบ
2026-07-26  2026-07-26_student-rpc-add-rate-limit.sql        ← ✅ ตัวจริงตอนนี้
```

**เกือบพลาดจริงเมื่อ 2026-07-31:** รายงานตรวจระบบชี้ไปที่ไฟล์ `2026-07-19` (ตัวแรก)
ถ้าแก้งาน C1 โดยลอกจากไฟล์นั้น → ทั้งค่า `'declined'` และยามเฝ้าประตู **จะถูกลบทิ้งไปพร้อมกันโดยไม่มีใครรู้**
จับได้เพราะเปิดเทียบทั้ง 3 ไฟล์ก่อน ไม่ได้เชื่อรายงาน

---

## 🔍 วิธีเช็คด้วยตัวเองว่า "ตอนนี้ในฐานข้อมูลจริงมีกี่ตัว"

เปิด Supabase → SQL Editor → วางแล้วกด Run (เปลี่ยนชื่อฟังก์ชันในบรรทัดสุดท้ายได้)

```sql
select p.proname,
       pg_get_function_identity_arguments(p.oid) as ช่องรับค่า
from pg_proc p
join pg_namespace n on n.oid = p.pronamespace
where n.nspname = 'public'
  and p.proname = 'submit_class_request';
```

**ต้องได้ 1 แถวเท่านั้น** · ได้ 2 แถวขึ้นไป = มีตัวซ้อนอยู่ ต้องลบตัวเกินทิ้งก่อน ไม่งั้นเว็บพัง
