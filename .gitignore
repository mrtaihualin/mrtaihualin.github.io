# ── ไฟล์ภายใน ห้ามเผยแพร่ / ไม่ push (ล็อกไว้) ──
# เอกสารแผนทั้งหมด (แผนหลัก + อ้างอิง)
_แผนงาน/
# หัวข้อบทความที่เตรียมไว้เขียนต่อ (ยังไม่ใช่หน้าเว็บจริง)
_บทความ-เตรียมเขียน/
# ไฟล์ทดสอบ/dev ที่ไม่จำเป็นต่อเว็บจริง
_dev/
# ฉบับเก่า เผื่อยังหลงเหลือ
📚 แผนระยะยาว.md
*.docx

# lead magnet — ยังไม่เปิดสาธารณะ รอตั้งระบบแลกอีเมลก่อน
lead-magnet-tones.html

# โฟลเดอร์แบ็คอัพ
_backup_*/

# ไฟล์ระบบ Mac
.DS_Store

# ไฟล์ขยะชั่วคราวจาก FUSE/mount (เกิดตอนแก้ไฟล์ขณะมี handle ค้าง) — ไม่ใช่ไฟล์เว็บจริง
.fuse_hidden*

# node_modules (ถ้ามี)
node_modules/

# คำสั่ง/playbook ความปลอดภัยภายใน — อยู่เครื่อง Lin เท่านั้น ห้าม push ขึ้น public repo
CLAUDE.md
assets/testimonials/IMG_C989003FFDD6-1.jpeg
_archive/

# ไฟล์ debug จาก AI sandbox หลุดมาติด repo — กันไม่ให้กลับมาอีก
check_old_tmp*
